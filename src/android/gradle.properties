# Project-wide Gradle settings.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2500m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Enable building projects in parallel
org.gradle.parallel=true

# Gradle caching allows reusing the build artifacts from a previous
# build with the same inputs. However, over time, the cache size will
# grow. Uncomment the following line to enable it.
#org.gradle.caching=true
androidBuildToolsVersion=33.0.1
androidCompileSdkVersion=31
androidNdkVersion=23.1.7779620
buildDir=build
qt5AndroidDir=/home/kebekus/Software/buildsystems/Qt/6.4.1/android_x86_64/src/android/java
qtAndroidDir=/home/kebekus/Software/buildsystems/Qt/6.4.1/android_x86_64/src/android/java
qtMinSdkVersion=23
qtTargetAbiList=x86_64
qtTargetSdkVersion=30
android.useAndroidX=true