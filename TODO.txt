
* Map does not work
* Donation: layout corrupt -- can scroll horizontally
* PositionInfoPage -- geht auf meinem Telefon über die Bildschirmbreite hinaus.

Check WaypointEditor (when keyboard comes up) and WeatherReport

MFM.qml:460
              var wp = global.geoMapProvider().closestWaypoint(flightMap.toCoordinate(Qt.point(mouse.x,mouse.y)),
                                                                 flightMap.toCoordinate(Qt.point(mouse.x+25,mouse.y)),
                                                                 global.navigator().flightRoute)
  
